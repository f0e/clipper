import React, { ReactElement } from 'react';
import './Edit.scss';

const Edit = (): ReactElement => {
	return (
		<main className="edit-page">
			<h1>todo</h1>
		</main>
	);
};

export default Edit;
